import { ObjectiveType } from '../Types/Objectives/ObjectivesType';

// value foi definida como undefined para que o conteúdo do placeholder possa aparecer, caso contrário o conteúdo do campo seria 0
export const initialObjectives: ObjectiveType = { id: 0, name: '', description: '', value: undefined };
